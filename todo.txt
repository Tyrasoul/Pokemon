1. Find a way to get input in one or two lines (if sepearting pokemon struct and envirnoment struct), if possible.
2. Handle Bad input stream states
3. Remove global vars if you want
4. Make a nice UI, in console only or otherwise
5. Change weathertype == pokemontype - to robust code